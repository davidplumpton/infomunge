%dw 2.0

fun toRadix(number, radix) = __native("toRadix", number, radix)
fun fromRadix(text, radix) = __native("fromRadix", text, radix)
fun toBinary(number) = toRadix(number, 2)
fun fromBinary(text) = fromRadix(text, 2)
